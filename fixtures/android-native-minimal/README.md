# Android Native Minimal Fixture

This fixture is not meant to build. It exists to validate the static USDK doctor checks against a project shape that should mostly pass.

Use it like this:

```bash
python scripts/usdk_doctor.py --project-root fixtures/android-native-minimal --pretty
```
